# Настройки для профиля prod
spring.application.name=demo
spring.datasource.url=jdbc:postgresql://postgres:5432/javademo
spring.datasource.username=postgres
spring.datasource.password=Egor100209
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

# Отключаем Liquibase для продакшн, используем init-db.sql
spring.liquibase.enabled=false

spring.redis.host=redis
spring.redis.port=6379

spring.data.redis.repositories.enabled=true

spring.kafka.bootstrap-servers=kafka:9092
spring.kafka.consumer.group-id=demo-group
spring.kafka.consumer.auto-offset-reset=earliest

kafka.topic.example=example-topic 